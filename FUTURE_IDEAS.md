# Future Ideas (not in v1 — parked on purpose)

Ideas go here instead of into the code, until the current build is shipped and working end to end.

- Eyes on the ghost/drop-preview piece — currently intentionally left as a plain translucent
  outline to keep the drop-landing spot easy to read; revisit if playtesting says otherwise
- Real app icon pass — current icon is a placeholder programmatically-generated jelly face;
  worth a proper design pass once this ships
- Blink animation (occasional quick eye-close) layered on top of the existing squish/dance idle
- "Challenge link" — encode a score into the share URL (e.g. ?beat=8827) so a shared link can
  show "beat this score" messaging without needing any server or leaderboard
- Apple App Store build via Capacitor, once there's budget for a developer account
